def Enumdict(listed):
    myDict = {}
    for i in enumerate(listed):
        #  myDict[x] = i
        # print (i)
        print (i)

    return myDict

indexes = ['alpha', 'beta', 'zeta']
# print (enumerate(indexes))
Enumdict(indexes)

print indexes.values()
